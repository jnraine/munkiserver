<% unless pkgs.empty? %>
	<h4>Recent packages</h4>
		<div class="box">
			<% pkgs.each do |pkg| %>
				<%= pkg.to_s(:pretty_with_version) %>
				[<%= link_to "show", pkg %>] [<%= link_to "edit", edit_package_path(pkg) %>]<br />
			<% end -%>
	</div>
<% end -%>