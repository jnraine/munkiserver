<% item_list.each do |item| %>
	<div class="packageListItem">
		<%= image_tag item.package.icon.url(:tiny) if item.package.icon %>
		<% if item.package_id.blank? %>
			<span><%= link_to item.package, package_path(current_unit, item.package) %></span>
		<% else %>
			<span><%= link_to item.package.to_s(:pretty_with_version), package_path(current_unit, item.package) %></span>
		<% end %>
		<% if target.respond_to?(:installed?) and target.installed?(item.package) %>
			<span class="installed-mark">&nbsp;&#10003;</span>
		<% end %>
	</div>
<% end %>